# swkBot2.0
swk bot 2.0 for discord

# usefull bot commands

forever start index.js 	-> runs bot
forver list 			-> lists process running with forever
forever stop id 		-> stops process

node deploy-commands.js -> adds new commands to the bot

# deployment to server

npm install forever -g -> in order to have node running in the background

